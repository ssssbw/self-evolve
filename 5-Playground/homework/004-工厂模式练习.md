# 作业 #004 — 工厂模式练习

> 前置阅读：Refactoring.Guru「工厂方法」+「抽象工厂」

---

## 题目一（必做）

**用工厂方法模式实现一个跨平台对话框：**

要求：
- 定义 `DialogButton` 和 `Dialog` 接口
- 实现 `IOSDialog` 和 `MaterialDialog` 两种风格
- 写一个 `createDialog(Platform platform)` 工厂函数
- 用 Dart 实现

---

## 题目二（必做）

**用抽象工厂实现一个主题切换系统：**

要求：
- 主题提供：Button 样式、Card 样式、AppBar 样式
- 实现 LightTheme 和 DarkTheme 两个具体工厂
- 写一个 Widget，接收 ThemeFactory，根据主题渲染不同 UI
- 用 Dart 实现

---

## 题目三（选做）

**在 Flutter SDK 中找到一个工厂方法模式的例子：**

要求：
- 写出具体的类名和方法名
- 解释为什么这是工厂方法而不是普通方法
- 画出简化版的类图（用文字描述）

---

## 提交

在本文件下方作答，或创建 `004-answer.md`。
