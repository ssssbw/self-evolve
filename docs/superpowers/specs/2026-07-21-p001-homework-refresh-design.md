# P001 当日题目更新设计

## 背景

用户已明确当前技术学习进度：

- CSAPP：第 1 章 + 2.1
- 设计模式：今天预计读完设计原则

现有 `P001` 题目中：

- `CSAPP/homework/001` 与 `CSAPP/discussions/001` 只覆盖第 1 章；
- `设计模式与架构/homework/001` 与 `discussions/001` 已进入单例与 immutable Widget，早于当前“设计原则”进度；
- `2026-W30.md` 尚未记录今天这组实际学习范围。

## 目标

- 不新增题号，只更新现有 `001` 题目，使其对齐当前进度。
- 保持“技术类可布置进阶作业，讨论题用于开放思考”的规则。
- 同步更新本周周复盘中的今天记录入口。

## 设计

### CSAPP

- `homework/001` 调整为“系统漫游 + 2.1 信息表示入门”的进阶理解作业。
- 重点从“链路复述”扩展到“同一串 bit 需要上下文解释”“表示方式如何影响工程判断”。
- `discussions/001` 调整为围绕“为什么应用层开发者也要关心位模式和上下文”展开。

### 设计模式

- `homework/001` 从单例实现前移为“设计原则识别与应用”。
- `discussions/001` 从 immutable Widget 前移为“为什么先学原则，再学模式”。
- 保持题型不变：作业要求结合代码场景，讨论题不设标准答案。

### 周记录

- `2026-W30.md` 只更新周二一行。
- 不伪造学习时长或完成结果，仅记录当天计划范围与已更新的题目入口。

## 非目标

- 不新增 `002+` 题目。
- 不回写其它历史周文件。
- 不调整 P001 长期路线图。

## 验证

- 5 个现有文件内容与当前学习进度一致。
- `git diff --check` 通过。
- `rg` 复查题目标题和周二记录，确认没有保留“单例”“immutable Widget”这类超前入口。
