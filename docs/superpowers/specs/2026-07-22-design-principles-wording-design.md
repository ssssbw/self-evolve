# 设计原则表述统一设计

## 背景

当前仓库在 `P001` 计划、当前设计原则作业和 `A1` 技术路线图中，把 `SOLID` 与 `DRY / KISS / YAGNI` 并列写成同一组“设计原则”。这会让人误以为存在一个固定的“八大设计原则”集合。

实际更严谨的层级是：

- `SOLID`：5 个核心面向对象设计原则
- `DRY / KISS / YAGNI`：常用通用工程原则

## 目标

- 统一当前执行面与长期路线图中的表述口径。
- 不改变学习顺序，不改变题号，不改变作业结构。
- 只修正概念层级与措辞。

## 变更范围

- `0-Projects/P001-基础重建/plan.md`
- `5-Playground/P001-基础重建/设计模式与架构/homework/001-设计模式单例练习.md`
- `1-Areas/A1-技术/roadmap.md`

## 统一表述

- `SOLID（5 个核心面向对象设计原则）`
- `DRY / KISS / YAGNI（通用工程原则）`

## 非目标

- 不扩展到全仓库所有历史文档。
- 不新增资源、阶段或题目。

## 验证

- 以上三处不再把 `SOLID` 与 `DRY / KISS / YAGNI` 混写成同一层级的固定集合。
- `git diff --check` 通过。
